# gross.ery

## CloudService .env

```
DB_URL=
DB_NAME=
APP_VERSION=
APP_DOWNLOAD_LINK=
```

## MobileApp environment

Copy `env.template.ts` and replace the appropriate values.

Required files:

```
env.dev.ts
env.prod.ts
```
